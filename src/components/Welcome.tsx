// Welcome was the single-page welcome screen from Increment 1.
// Its functionality is now split between:
//   - routes/Home.tsx (cert + mode picker)
//   - routes/Practice.tsx (length picker)
// This file is kept as a stub to avoid touching the file (Windows mount quirks).
export default function DeprecatedWelcome(): null {
  return null;
}
